version: '3.9'

services:
  db:
    image: postgres:16.0
    container_name: postgres_lms
    environment:
      - POSTGRES_DB=${NAME}
      - POSTGRES_USER=${USER}
      - POSTGRES_PASSWORD=${PASSWORD}